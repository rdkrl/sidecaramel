"""Package-bundled data files for sidecaramel.

Currently:
    cusswords.json — cuss-word regex patterns used by lrc_cusswords.
                     Override via $SIDECARAMEL_CUSSWORDS env var
                     pointing to an alternate JSON file.
"""
